import type { Metadata } from "next";

export const metadata: Metadata = {
    title: "Domluvit schůzku",
    description: "Domluvte si nezávaznou schůzku se SimCall — probereme vaše potřeby a navrhneme enterprise řešení na míru.",
};

export default function DomluvitSchuzkulayout({ children }: { children: React.ReactNode }) {
    return children;
}
