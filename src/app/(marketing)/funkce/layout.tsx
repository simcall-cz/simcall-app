import type { Metadata } from "next";

export const metadata: Metadata = {
    title: "Funkce",
    description: "Objevte všechny funkce SimCall. AI analýza hovorů, přepisy, personalizované scénáře, manager dashboard a export reportů.",
};

export default function FunkceLayout({ children }: { children: React.ReactNode }) {
    return children;
}
